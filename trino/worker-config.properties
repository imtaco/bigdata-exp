coordinator=false
http-server.http.port=8080
discovery.uri=http://trino:8080

# Query execution configuration for workers
#query.max-memory-per-node=2GB
#query.max-total-memory-per-node=2GB

#query.max-total-memory=2G
#query.max-memory-per-node=2GB

# Spill configuration
#spiller-spill-path=/tmp/trino-spill
#spill-enabled=true
#spill-compression-enabled=true
spiller-spill-path=/tmp/trino-spill
spill-enabled=true
spill-compression-codec=LZ4
spill-encryption-enabled=false

# Task configuration
task.concurrency=16
task.max-worker-threads=64

# Memory management
memory.heap-headroom-per-node=2GB
